# New-language (ps)

超軽量・爆速プログラミング言語。

## 構文デザイン (第1版)
* `ps "Hello"` で画面に出力（printやカッコ、セミコロンはだるいので廃止）
* `x = 10` で自動で型を推論して記憶

## 裏側の仕組み
* 人間が書いたシンプルなコードを、裏でAIがPCに優しい爆速C++コードに変換
* Renderサーバー上で自動コンパイルして実行
